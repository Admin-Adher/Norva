# Blog review — 10 September 2026

Historical preparation record. Branch: `codex/blog-resume-20260910`, based on `fbb1ba05bda13ac9a13b6f7d504638482bc69555`. Nothing was pushed or deployed during that preparation, and a fully tested product walkthrough was not claimed. The later publication authorization and fresh release checks are recorded below.

## Review the seven pages

Local preview starts at `http://127.0.0.1:4179/blog/`. The preview server binds only to localhost, sends `noindex, nofollow`, permits only GET/HEAD, and blocks external script/image/connect destinations with CSP.

| Article | Concrete addition | Preview path |
|---|---|---|
| First session | Real connection form, Ready follow-up, language-filter example, explicit verification boundaries | `/blog/norva-getting-started/` |
| Connect a source | Five real connection screenshots, one current filter screenshot, tested menu path and catalogue checkpoints | `/blog/connect-compatible-media-source-norva/` |
| Organise a collection | Current source/audio-filter walkthrough plus a clearly fictional five-record example | `/blog/organize-large-movie-collection/` |
| What is Norva? | Real empty source form, source-fit comparison and latest catalogue observations | `/blog/what-is-norva-media-player/` |
| Captions/subtitles | Original off-screen-speaker/door-knock illustration and coverage comparison | `/blog/captions-and-subtitles-why-the-accessibility-goals-can-differ/` |
| Legibility/readability | Same-word contrast and grouping specimens; no invented usability result | `/blog/legibility-and-readability-two-different-viewing-problems/` |
| Resolution/bitrate | Controlled 1080p, 4/8 Mbps illustration with 300/600 MB arithmetic | `/blog/resolution-and-bitrate-why-they-are-not-the-same/` |

Six real JPEG screenshots and three authored SVG illustrations are referenced. Historical isolated-component PNGs are retained separately but not used in these articles. All screenshot credentials and private connection details are absent. The public text preserves the player-without-included-content positioning.

## Editorial and technical changes

- The generic draft warning is replaced locally by a precise description of what was tested and what was not. Human-review status remains pending; no reviewer or successful playback has been invented.
- The shared renderer produces one Sources section, retaining curated labels and adding only missing metadata URLs. The authored next step takes precedence over the duplicate template CTA.
- Figures have descriptive alternatives, captions and full-size links. A rendered check exposed zero-width lazy images with the previous `width: auto` rule; `width: 100%` now reserves space from the intrinsic dimensions before image bytes arrive. A regression test protects this behaviour.
- Existing Norva typography, colours and starfield are preserved. No SPA, Android, language-parser or playback code was changed by this blog resumption.
- Reading-time estimates for the revised copy were refreshed using approximately 200 words/minute. First-publication dates, calendar and human-approval fields were not changed.
- The recovered measurement patch distinguishes blog views/clicks from real funnel outcomes, respects consent, and keeps a bounded same-tab click context. Its tests do not prove live GA4 reporting. Administration, custom dimensions, real consent/auth acceptance and Ads settings are unchanged; see `docs/analytics/blog-measurement-readiness.md`.

## Current checks

| Check | Result |
|---|---|
| Corpus parsing/conversion | 1,000 articles; 0 problems |
| Final focused renderer/blog/measurement/funnel tests | 31 passed; 0 failed |
| Existing-only rebuild | 117 existing articles; repeat wrote 0 pages |
| Publication state and calendar | Unchanged against branch baseline |
| Local HTTP article checks | 7/7: HTTP 200, one Sources section, one primary CTA, no old generic draft warning |
| Referenced image checks | 9/9: HTTP 200, correct dimensions and byte-for-byte match with local files |
| Responsive DOM checks | 7 pages × 2 sizes: 390 × 844 and 1440 × 1000 CSS pixels; no horizontal page overflow, all TOC targets present, images reserve non-zero dimensions |
| Actual navigation | Ready/source filter/clear/search/details/Back in the test account; article TOC; organisation CTA opened the intended broad-to-narrow article |
| Console spot checks | No warning/error captured on the checked local article/CTA flow |
| Broader Node suite, before final CSS-only regression | 4,026 passed, 4 failed, 5 skipped, 4,035 total; not an all-green result |

The four broad-suite failures concern the Android CI evidence assertion, a gateway child process missing `express`, a gateway complete-cache fixture returning HTTP 502, and a Revolut sandbox bootstrap test unable to parse its subprocess output. The affected implementation/test files were not edited here. These failures were not bypassed and must not be represented as fixed or causally attributed to the blog without a separate baseline comparison. Dependencies were reused through NODE_PATH; no install or remote financial operation was performed.

The browser's existing zoom was preserved. Responsive sizes above were verified from `innerWidth/innerHeight`, not assumed from an emulation setting. Some emulated screenshot exports included unused dark canvas; they are not clean device screenshots. A normal 1600 × 900 desktop render and the real 869 × 975 filter capture were inspected without that limitation. This is browser QA, not native Android or WCAG certification.

Opening the three SVGs as standalone documents subsequently produced three animation-related promise errors without a source URL, plus one explicitly attributed to a browser extension. These are recorded in `console-review.json`, not silently counted as clean. A fresh article reload after 17:43:19 UTC produced no new warning/error; the unlocated SVG messages have not been diagnosed or labelled as application failures.

## Product and release boundaries

The source now displays Ready and the known title has twelve visible versions. That is not an audit of every file's metadata or a successful playback session. A later catalogue opening confirmed the test favourite persisted; removal and restoration of the original state were confirmed after a full reload. Immediate favourite feedback was not accepted. No playback was retried, no source was duplicated and no access dates were added. The interface language was restored to Automatic after user-approved English captures, and the search was cleared before returning to Series.

Before release: review these previews and their verification wording, explicitly approve publication, resolve/report the broader suite gates, and complete the separately authorised analytics acceptance. Do not mark the source frontmatter reviewed or product claims fully verified merely because local rendering passes.

## Evidence files

Working tree: `C:/Users/AdrienHernandez/.codex/worktrees/norva-blog-resume-20260910`.

Local QA directory: `C:/Users/AdrienHernandez/.codex/tmp/norva-blog-resume-20260910`:

- `browser-qa.json`: fourteen responsive DOM observations.
- `artifact-verification.json`: article checks, image dimensions, SHA-256 hashes and frozen publication state.
- `full-tests.log`: full-suite result, including the four failures.
- `console-review.json`: standalone-SVG diagnostics and clean follow-up article reload.
- `tutorial-desktop.jpg`: normal desktop section inspection.
- `resolution-mobile.jpg`: responsive export with the capture-tool canvas limitation above.
- `preview-server.cjs` and `verify-artifacts.cjs`: reproducible local-only checks.

New real filter-image SHA-256: `3cea26c2411cfac4aef83dba4ee114cc0a136a7d4e38866d7b4a7a048951b0d1`.

## Publication authorization and fresh release gates — 10 September 2026

The user explicitly requested production publication of this prepared lot. The release was assembled in the separate `codex/blog-publish-20260910` worktree from freshly fetched `origin/main` at `fbb1ba05`. The original application checkout and preparation worktree were preserved. Only the seven source articles, shared blog rendering/styles, bounded marketing instrumentation, nine referenced assets and their tests/documentation were carried over; the three unused component-harness PNGs were excluded.

After installing the lockfile with `npm ci --ignore-scripts --no-audit --no-fund`, the full Node suite returned **4,030 passed, zero failed, 10 skipped, 4,040 total** (`--test-concurrency=4`). The previous four failures did not recur; no unrelated product or test fix was needed. The focused blog/measurement/consent/Ads/checkout suite passed **49/49**. Corpus validation parsed 1,000 articles without problems; locale validation, region validation, syntax checks and compilation of the pinned Cloudflare Pages Functions passed. These are local pre-publication results, not evidence of deployment or live analytics acceptance.

The frozen rebuild contains exactly **117 previously published articles** and a second run writes zero pages. First-publication state and calendar remain unchanged. All 117 receive shared Sources/CTA/rendering fixes; only seven receive the deeper editorial revision and illustrations. The other 993 source drafts were not individually rewritten or fact-checked by this release. Explicit publication approval does not turn the documented untested playback/native flows into accepted tests, nor does it authorize new GA4/Ads settings, consent actions or financial transactions.

Release verification must identify the exact pushed commit, successful Cloudflare workflow and matching public article/image/asset responses. The GitHub workflow repeats the full regression suite on Ubuntu/Node 22 before deploying and applies the existing cache-busting step to all consumers of `marketing.js`.
