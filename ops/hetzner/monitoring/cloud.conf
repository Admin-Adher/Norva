# Disable the Netdata Cloud connection (ACLK) entirely.
# We run 100% self-host: no claiming, no phone-home to app.netdata.cloud, and
# the dashboard stops showing the "connect your agent / Claim" prompt.
# Read from /var/lib/netdata/cloud.d/cloud.conf (bind-mounted by the compose).
[global]
    enabled = no
