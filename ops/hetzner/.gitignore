# Never commit filled secrets, DB dumps, or runtime state
.env
dump/
postgres/userlist.txt
# Runtime data dirs (if ever created under the repo instead of /var/lib/norva)
volumes/db/data/
volumes/storage/
