<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Google Play Billing (in-app subscriptions via RevenueCat). -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Offline downloads run in a foreground service with a progress notice. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Advertising ID: used by Firebase/Google Analytics for Google Ads App campaign
         measurement. Required by Google Play for apps that use the ad ID on Android 13+ (API 33). -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>

    <application
        android:name=".NorvaApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <!-- Native analytics is opt-in. Firebase remains disabled until the
             origin-scoped consent bridge applies the stored user choice. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.NorvaSplash"
            android:screenOrientation="portrait">

            <!-- Launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- norva:// deep link for QR pairing -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="norva" android:host="pair" />
            </intent-filter>

            <!-- https App Links: a shared norva.tv title/app link opens the app.
                 autoVerify requires /.well-known/assetlinks.json on norva.tv with
                 the release-signing SHA-256 (see public/.well-known/assetlinks.json);
                 until verified, Android still offers the app as a link handler. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="norva.tv" android:path="/app.html" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="norva.tv" android:pathPrefix="/t/" />
            </intent-filter>
            <!-- Norva Partners referral links stay on the trusted Norva origin.
                 MainActivity loads the canonical /r/{opaque-code} URL in the
                 cloud WebView so its server-side HttpOnly attribution cookie
                 survives native Google sign-in and the following auth route. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="norva.tv" android:pathPrefix="/r/" />
            </intent-filter>
            <!-- Didit returns to this exact Norva endpoint. Android opens the
                 trusted callback in the app, which strips provider query data
                 before the WebView follows Norva's no-store redirect. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="norva.tv" android:path="/partners-kyc-return" />
            </intent-filter>

        </activity>

        <activity
            android:name=".PlayerActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="false"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:theme="@style/PlayerTheme"
            android:screenOrientation="sensorLandscape" />

        <!-- Offline downloads: native list of saved movies (the "Downloads" screen). -->
        <activity
            android:name=".DownloadsActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|uiMode"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Chromecast: Cast framework bootstrap (Default Media Receiver). -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="tv.norva.phone.CastOptionsProvider" />

        <!-- Foreground service that downloads + AES-encrypts media off the home network. -->
        <service
            android:name=".DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Firebase Cloud Messaging: receives "your catalog is ready" pushes (caches the
             device token, shows foreground notifications). exported=false — only FCM delivers here. -->
        <service
            android:name=".NorvaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
